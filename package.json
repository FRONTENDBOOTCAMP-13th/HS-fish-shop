{
  "name": "vite-css",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "rm -rf dist",
    "all": "run-s clean build preview"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "vite": "^6.2.1"
  }
}
